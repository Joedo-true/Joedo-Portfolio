# Joedo.dev — портфолио фронтенд-разработчика

Продающий одностраничный лендинг-портфолио с ощущением «сверх-технологичного музея»:
**React + TypeScript + Tailwind CSS + Framer Motion + Three.js**. Тёмная/светлая тема,
настоящий WebGL-3D в hero, живые интерактивные кейсы и проработка каждой детали.

![Stack](https://img.shields.io/badge/React-18-61DAFB) ![TS](https://img.shields.io/badge/TypeScript-5-3178C6) ![Tailwind](https://img.shields.io/badge/Tailwind-3-2DD4BF) ![Three.js](https://img.shields.io/badge/Three.js-r169-000000) ![Vite](https://img.shields.io/badge/Vite-5-646CFF)

## ✨ Возможности

- **Hero с настоящим 3D** — абстрактный «кристалл» на **Three.js / React Three Fiber**: светящееся
  морфящееся ядро, каркас-оболочка и парящие полиэдры со студийным светом, которые плавно следуют
  за курсором. Есть аккуратный CSS-фолбэк при отсутствии WebGL или `prefers-reduced-motion`.
- **Продающий оффер** — тег доступности со scarcity, сильный заголовок, две CTA, планка статистики
  с «набегающими» цифрами (счётчики) и бегущая строка ценностей.
- **Услуги** — блок-оффер из 4 форматов (лендинги, калькуляторы, дашборды, магазины) на карточках
  с 3D-наклоном к курсору и бликом.
- **Кейсы** — 3 бизнес-кейса в чередующейся раскладке, каждый — **живой интерактивный интерфейс**:
  - AutuDash — дашборд на **Recharts** с кликабельными графиками и таблицей заказов;
  - Sollers Shop — каталог с фильтром по цене (слайдер), категориями и живой корзиной;
  - FlexiCalc — многошаговый калькулятор-квиз с расчётом стоимости на лету.
- **Стек** — плиточная сетка (Core / UI-UX / Build & Deploy); плитка окрашивается в фирменный цвет.
- **Процесс** — горизонтальная timeline из 4 шагов (вертикальная на мобильных).
- **Контакты** — форма из 3 полей с градиентной подсветкой рамки, «магнитная» кнопка и быстрые
  ссылки в Telegram / Email.
- **Музейная проработка** — плёночная зернистость поверх всего, кастомный курсор, дрейфующая
  «аврора», стеклянные панели с градиентными рамками, «экспонатное» нумерование блоков.
- **Тёмная тема** — глубокий near-black графит со слоями света; **светлая** — чистая молочно-белая.
- **Адаптивность** — от маленького смартфона до широкого десктопа, без горизонтального скролла.
- Уважается системная настройка `prefers-reduced-motion`.

## 🚀 Запуск

### Вариант 1 — просто открыть в браузере (без установки)

В репозитории уже лежит готовый **self-contained файл** —
[`standalone/index.html`](standalone/index.html). Весь сайт (React, стили, графики)
встроен в один HTML-файл. Ничего ставить и собирать не нужно:

> **Скачайте `standalone/index.html` и откройте его двойным кликом в браузере.**

Работает по протоколу `file://`, без сервера. Единственное, что подгружается из
сети, — красивый шрифт Inter (при отсутствии интернета подставится системный).

Пересобрать этот файл после правок в коде:

```bash
npm install
npm run build:standalone   # → standalone/index.html
```

### Вариант 2 — полноценная разработка (Vite)

```bash
npm install
npm run dev      # локальная разработка → http://localhost:5173
npm run build    # прод-сборка в dist/ (для Vercel/Netlify)
npm run preview  # предпросмотр прод-сборки
```

> ⚠️ Файл `index.html` в корне — это точка входа для Vite, его нельзя открыть
> напрямую двойным кликом (он подключает исходники через сборщик). Для открытия
> «как есть» используйте `standalone/index.html`.

## ⚙️ Настройка под себя

Все контакты и мета-данные вынесены в один файл — **`src/data/site.ts`**
(Telegram, Email, GitHub, пункты навигации). Контент проектов — в `src/data/projects.ts`,
навыки — в `src/data/skills.ts`, шаги процесса — в `src/data/workflow.ts`.

Форма отправки собирает письмо и открывает почтовый клиент (`mailto:`) — бэкенд не требуется.
Для реальной отправки подключите свой обработчик (Formspree, Telegram Bot API, серверлесс-функцию)
в `src/components/Contact.tsx`.

## 🌐 Деплой

### GitHub Pages (настроено ✅)

В репозитории уже лежит workflow `.github/workflows/deploy.yml`, который
автоматически собирает сайт и публикует его на GitHub Pages при каждом пуше
в основную ветку. Нужно один раз включить Pages:

1. Откройте в репозитории **Settings → Pages**.
2. В разделе **Build and deployment → Source** выберите **GitHub Actions**.
3. Готово. После следующего пуша (или запустите workflow вручную:
   вкладка **Actions → Deploy to GitHub Pages → Run workflow**) сайт появится по адресу:

   **`https://joedo-true.github.io/Joedo-Portfolio/`**

Пути к ресурсам относительные (`base: './'`), поэтому сайт корректно работает
из подпапки `/Joedo-Portfolio/` без дополнительных настроек.

### Vercel / Netlify

Тоже из коробки: команда сборки `npm run build`, каталог публикации `dist`.

## 🧱 Технологии

`React 18` · `TypeScript 5` · `Vite 5` · `Tailwind CSS 3` · `Framer Motion 11` ·
`Three.js r169` · `@react-three/fiber` · `@react-three/drei` · `Recharts 2`
